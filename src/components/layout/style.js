import { makeStyles } from "@material-ui/styles";

const useStyle = makeStyles({
  headerDiv: {
    display: "flex",
  },
  footer: {
    display: "flex",
    justifyContent: "end",
  },
});
export default useStyle;
